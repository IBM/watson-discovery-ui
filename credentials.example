# Replace the credentials here with your own.
# Rename this file to .env before starting the app.

# Watson Discovery
DISCOVERY_USERNAME= {{ bluemix.discovery.username }}
DISCOVERY_PASSWORD= {{ bluemix.discover.password }}
DISCOVERY_ENVIRONMENT_ID= {{ bluemix.discover.environment_id }}
DISCOVERY_COLLECTION_ID= {{ bluemix.discover.collection_id }}

# Run locally on a non-default port (default is 3000)
# PORT=3000
